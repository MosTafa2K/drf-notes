schemaVersion: "1.2"
components:
  - name: rest-api-be2
    source:
      type: local
      path: backend  # یا اگر مسیر دیگری استفاده می‌شود، آن را تنظیم کن
    endpoints:
      - name: rest-api-be2
        displayName: REST API
        service:
          basePath: /
          port: 8000
        type: REST
        networkVisibilities:
          - Project
          - Public
